version: "3"

tasks:
  test:
    cmds:
      - |
          bash <<'EOF'
          set -euo pipefail
          source ../../_lib/run.sh

          go build -trimpath -ldflags="-s -w" -o bin/core-agent ../../../../cmd/core-agent

          output="$(mktemp)"
          # session/list calls the API — exit 1 with error is expected offline
          run_capture_all 1 "$output" ./bin/core-agent session/list
          assert_contains "session" "$output"
          EOF
