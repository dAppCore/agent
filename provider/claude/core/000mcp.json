{
  "mcpServers": {
    "core": {
      "type": "stdio",
      "command": "core-agent",
      "args": ["mcp"],
      "env": {
        "MONITOR_INTERVAL": "15s",
        "CORE_AGENT_DISPATCH": "1"
      }
    }
  }
}
